// ----------------------------------- Practice JS #1 -------------------------------------
const camelCase = 0;
const snake_case = 1;
const PascalCase = 2;

//alert() останавливает все взаимодействие с браузером до тех пор, пока сообщение не будет отклонено, 
//в то время как console.log() просто выводит сообщение на консоль.

//Например, использую alert() необходимо закрывать каждый раз окно вывода результата, чтобы увидеть вывод следующего

console.log(camelCase);
console.log(snake_case);
console.log(PascalCase);

alert(camelCase);
alert(snake_case);
alert(PascalCase);
// -------------------------------- end region ---------------------------------------------